apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../../../components/export-format

resources:
  - ../../base

patches:
  - target:
      kind: CronWorkflow
      name: data-export-crm-services
    path: data-export-crm-services.yaml
