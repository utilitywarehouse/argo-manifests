apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: WorkflowTemplate
      name: flow-billing-bill-account
    path: labels.yaml
