apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - resolve-account.yaml

# No image pins: resolve-account runs its query through step-query, which owns
# the query-pg pin in shared/kustomization.yaml.
