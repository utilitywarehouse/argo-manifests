# =============================================================================
# resolve-account — account-platform leaf: account number → account_id
# =============================================================================
#   in : account-number  (one, or comma-separated many)
#   out: account-id      (uuid; comma-joined when many numbers were given)
#
# One stage of the account pipeline: flow-test-account-select pipes account-id
# into step-energy-resolve-service-identifiers for the account's supplies.
# Submittable on its own too. See docs/data-flow.md.
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: step-account-resolve-account
  namespace: qe-argo-workflows
  annotations:
    workflows.argoproj.io/description: >-
      account-platform lookup: account-number → account_id (the `account-id` param).
      Submittable, and consumed via templateRef by the account pipeline.
  labels:
    app.kubernetes.io/component: step
    workflows.argoproj.io/creator: qe
    workflows.argoproj.io/invocation: standalone
    data.uw.systems/system: account
spec:
  entrypoint: resolve
  serviceAccountName: workflow-step-executor
  arguments:
    parameters:
      - { name: account-number } # one, or comma-separated many (number IN (...))
  templates:
    - name: resolve
      inputs:
        parameters:
          - { name: account-number }
          - { name: account-cluster-name, value: dev-aws }
          - { name: account-namespace, value: account-platform }
          - { name: log-level, value: info }
          - { name: teardown, value: "true" }
      outputs:
        parameters:
          - name: account-id # customer_account_id; comma-joined when many numbers were given
            valueFrom:
              parameter: "{{steps.account-platform.outputs.parameters.result}}"
      steps:
        # account-platform: account-number → account_id (point lookup on `number`).
        - - name: account-platform
            templateRef: { name: step-query, template: pg }
            arguments:
              parameters:
                - {
                    name: cluster-name,
                    value: "{{inputs.parameters.account-cluster-name}}",
                  }
                - {
                    name: target-namespace,
                    value: "{{inputs.parameters.account-namespace}}",
                  }
                - { name: job-name, value: resolve-account-id }
                - name: query
                  value: >-
                    SELECT id::string AS id FROM accounts
                    WHERE account_number IN (SELECT unnest(string_to_array('{{inputs.parameters.account-number}}', ',')))
                - { name: format, value: csv }
                - { name: output, value: /tmp/out.csv }
                - { name: assert, value: "!empty" }
                - name: assert-message
                  value: "resolve-account: no accounts rows for account-number(s)={{inputs.parameters.account-number}}"
                - { name: result-expr, value: 'join(map(rows, .id), ",")' }
                - { name: db-type, value: cockroachdb }
                - name: connection-string
                  value: "postgresql://accounts@cockroachdb-proxy:26257/account_api_20250214?sslmode=verify-full"
                # cockroachdb wants a client cert; the CA mints one in-cluster.
                - { name: ca-secret, value: ca-auth-key }
                - { name: log-level, value: "{{inputs.parameters.log-level}}" }
                - { name: teardown, value: "{{inputs.parameters.teardown}}" }
