apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: executor-remote-namespace
spec:
  templates:
    - name: execute-job
      inputs:
        parameters:
          - name: cluster-name
          - name: cluster-server
          - name: target-namespace
          - name: job-template
          - name: timeout
            value: "5m"
          - name: teardown
            value: "true"
          - name: debug
            value: "false"
      outputs:
        artifacts:
          - name: result
            path: /tmp/result.txt
            archive:
              none: {}
          - name: logs
            path: /tmp/logs.txt
            archive:
              none: {}
        parameters:
          - name: result
            valueFrom:
              path: /tmp/result.txt
      volumes:
        - name: job-executor-tokens
          secret:
            secretName: job-executor-tokens
        - name: cluster-ca-certs
          configMap:
            name: cluster-ca-certs
      script:
        image: bitnami/kubectl:latest
        volumeMounts:
          - name: job-executor-tokens
            mountPath: /secrets/tokens
          - name: cluster-ca-certs
            mountPath: /certs
        command: [sh]
        source: |
          set -e

          CLUSTER={{inputs.parameters.cluster-name}}
          CLUSTER_SERVER={{inputs.parameters.cluster-server}}
          K8S_USER="argo-workflow-job-executor"
          NAMESPACE={{inputs.parameters.target-namespace}}
          TIMEOUT={{inputs.parameters.timeout}}
          TEARDOWN={{inputs.parameters.teardown}}
          DEBUG={{inputs.parameters.debug}}
          WORKFLOW_NAME="{{workflow.name}}"
          WORKFLOW_UID="{{workflow.uid}}"
          POD_NAME="{{pod.name}}"
          TEMPLATE_NAME=$(echo $POD_NAME | sed "s/^${WORKFLOW_NAME}-//")
          JOB_NAME="{{steps.name}}-$TEMPLATE_NAME"
          TOKEN_KEY="${CLUSTER}-${NAMESPACE}"

          touch /tmp/result.txt
          touch /tmp/logs.txt

          log() { [ "$DEBUG" = "true" ] && echo "$@"; }

          echo "Cluster:   $CLUSTER"
          echo "Namespace: $NAMESPACE"
          echo "Job:       $JOB_NAME"

          export KUBECONFIG=/tmp/kubeconfig
          kubectl config set-credentials $K8S_USER \
            --token="$(cat /secrets/tokens/$TOKEN_KEY)" >/dev/null 2>&1
          kubectl config set-cluster $CLUSTER \
            --server=$CLUSTER_SERVER \
            --certificate-authority=/certs/${CLUSTER}-ca.crt >/dev/null 2>&1
          kubectl config set-context $CLUSTER \
            --cluster=$CLUSTER \
            --user=$K8S_USER \
            --namespace=$NAMESPACE >/dev/null 2>&1
          kubectl config use-context $CLUSTER >/dev/null 2>&1

          # Check namespace exists
          if ! kubectl get namespace $NAMESPACE >/dev/null 2>&1; then
            echo "ERROR: Namespace '$NAMESPACE' does not exist in cluster '$CLUSTER'"
            echo "Available namespaces:"
            kubectl get namespaces --no-headers -o custom-columns=":metadata.name"
            exit 1
          fi
          echo "✓ Namespace exists"

          # Build job manifest
          cat > /tmp/job.yaml <<'JOBEOF'
          {{inputs.parameters.job-template}}
          JOBEOF

          kubectl create -f /tmp/job.yaml --dry-run=client -o yaml | \
            sed "/^  generateName:/d" | \
            sed "/^  name:/d" | \
            sed "/^  namespace:.*/d" | \
            sed "/^metadata:/a\  name: ${JOB_NAME}\n  namespace: ${NAMESPACE}\n  labels:\n    workflow-name: ${WORKFLOW_NAME}\n    workflow-uid: ${WORKFLOW_UID}" \
            > /tmp/job-final.yaml

          log "Job manifest:"
          log "$(cat /tmp/job-final.yaml)"

          kubectl create -f /tmp/job-final.yaml -n $NAMESPACE >/dev/null 2>&1
          echo "✓ Job created"

          echo "Waiting for completion (timeout: $TIMEOUT)..."
          kubectl wait --for=condition=complete job/$JOB_NAME -n $NAMESPACE --timeout=$TIMEOUT >/dev/null 2>&1
          echo "✓ Job completed"

          kubectl logs job/$JOB_NAME -n $NAMESPACE > /tmp/logs.txt

          log "Job logs:"
          log "$(cat /tmp/logs.txt)"

          # Use ARGO_RESULT_OUTPUT if set, otherwise fall back to full logs
          RESULT=$(sed -n 's/^ARGO_RESULT_OUTPUT=//p' /tmp/logs.txt)
          if [ -n "$RESULT" ]; then
            echo "$RESULT" > /tmp/result.txt
          else
            cp /tmp/logs.txt /tmp/result.txt
          fi

          echo "Result: $(cat /tmp/result.txt)"

          [ "$TEARDOWN" = "true" ] && kubectl delete job/$JOB_NAME -n $NAMESPACE >/dev/null 2>&1 && echo "✓ Job cleaned up"

          rm -rf /tmp/kubeconfig
