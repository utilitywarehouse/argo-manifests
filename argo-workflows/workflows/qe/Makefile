include ../../conventions.mk

PKG=qe

# Only the overlay is deployable; base alone has no environment.
KUSTOMIZE_DIR=overlays/dev-aws

# templateRefs resolve only in an environment build.
LINTABLE=no
